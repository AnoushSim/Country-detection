##User registration with country detection by IP

User registration is the backend API , written in Node.js.

**RUN**
 * node server.js
 * open POSTMAN
 * type "localhost:1234/" and one of api endpoints (for example 'localhost:1234/registration') 
